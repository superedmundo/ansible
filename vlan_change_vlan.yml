---
- name: Change Vlan Port
  hosts: 10.100.1.59
  gather_facts: false
  vars:
    tag: "something"



  tasks:
    - name: Change Vlan Port
      debug:
        msg: HolaCrayola

    - name: Gather listed ntp config
      cisco.ios.ios_ntp_global:
        state: gathered
      register: ntp_config 